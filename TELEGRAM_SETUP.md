# Настройка Web-App в Telegram

## Шаг 1: Убедитесь, что GitHub Pages работает

1. Откройте в браузере: **https://zoombar.github.io/pershcoin/**
2. Должна открыться страница с кнопкой тапа
3. Если страница не открывается - проверьте настройки GitHub Pages (Settings → Pages)

## Шаг 2: Настройте Web-App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду: `/mybots`
3. Выберите вашего бота из списка
4. Выберите **"Bot Settings"**
5. Выберите **"Menu Button"**
6. Выберите **"Configure menu button"**
7. Введите название кнопки (например: `🎮 Играть` или `Play`)
8. Введите URL вашего web-app: `https://zoombar.github.io/pershcoin/`
9. Готово! Кнопка появится в меню бота

## Шаг 3: Обновите API_BASE в script.js

Сейчас в `webapp/script.js` указан локальный адрес. Нужно обновить на URL вашего API сервера.

### Вариант A: Локальная разработка (с ngrok)

1. Установите ngrok: https://ngrok.com/download
2. Запустите бота: `python main.py`
3. В другом терминале запустите ngrok: `ngrok http 8080`
4. Скопируйте HTTPS URL от ngrok (например: `https://abc123.ngrok.io`)
5. Обновите `webapp/script.js`:

```javascript
const API_BASE = 'https://abc123.ngrok.io'; // Замените на ваш ngrok URL
```

6. Обновите файл в ветке gh-pages и отправьте на GitHub

### Вариант B: Продакшен (сервер)

1. Разверните бота на сервере (VPS, Heroku, Railway, и т.д.)
2. Настройте домен для API (например: `api.yourdomain.com`)
3. Обновите `webapp/script.js`:

```javascript
const API_BASE = 'https://api.yourdomain.com'; // Замените на ваш API URL
```

4. Обновите файл в ветке gh-pages и отправьте на GitHub

## Шаг 4: Обновите web-app на GitHub Pages

После изменения `script.js`:

```bash
# Переключитесь на ветку gh-pages
git checkout gh-pages

# Скопируйте обновленный script.js из webapp/
Copy-Item ..\webapp\script.js . -Force

# Или отредактируйте script.js напрямую в этой ветке

# Закоммитьте и отправьте
git add script.js
git commit -m "Update API_BASE URL"
git push origin gh-pages

# Вернитесь на main
git checkout main
```

## Шаг 5: Проверьте работу

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню (три линии внизу слева)
3. Должна появиться кнопка "🎮 Играть" (или как вы назвали)
4. Нажмите на кнопку - откроется web-app
5. Попробуйте тапнуть - должно работать!

## ⚠️ Важные моменты

### 1. Бот должен быть запущен

Web-app не будет работать без запущенного бота, так как он обращается к API:

```bash
python main.py
```

### 2. API должен быть доступен

- Для локальной разработки: используйте ngrok
- Для продакшена: настройте сервер с HTTPS

### 3. CORS настройки

Убедитесь, что ваш API сервер разрешает запросы с домена GitHub Pages:
- Домен: `zoombar.github.io`
- В коде уже есть CORS middleware, но проверьте настройки сервера

### 4. HTTPS обязателен

- GitHub Pages работает по HTTPS ✅
- Ваш API сервер тоже должен работать по HTTPS
- Telegram Web Apps требуют HTTPS

## 🔧 Быстрая проверка

1. ✅ GitHub Pages работает: https://zoombar.github.io/pershcoin/
2. ✅ Кнопка настроена в BotFather
3. ✅ API_BASE обновлен в script.js
4. ✅ Бот запущен (`python main.py`)
5. ✅ API доступен (ngrok или сервер)
6. ✅ Web-app обновлен на GitHub Pages

## 📝 Чеклист

- [ ] GitHub Pages включен и работает
- [ ] Web-app настроен в BotFather
- [ ] API_BASE обновлен в script.js
- [ ] Бот запущен локально или на сервере
- [ ] API доступен (ngrok или сервер)
- [ ] Изменения отправлены в ветку gh-pages
- [ ] Проверено в Telegram - кнопка открывает web-app

## 🚀 Готово!

После выполнения всех шагов ваш web-app будет работать в Telegram!
