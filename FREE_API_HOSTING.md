# Бесплатный хостинг API для GitHub Pages

Поскольку GitHub Pages может хостить только статические файлы, вам нужен отдельный сервер для API. Вот лучшие **бесплатные** варианты:

## 🚀 Вариант 1: Railway.app (Рекомендуется)

**Бесплатно:** $5 кредитов в месяц (достаточно для небольшого проекта)

### Шаги:

1. **Зарегистрируйтесь на [Railway.app](https://railway.app)**
   - Войдите через GitHub

2. **Создайте новый проект:**
   - New Project → Deploy from GitHub repo
   - Выберите ваш репозиторий

3. **Настройте переменные окружения:**
   - `BOT_TOKEN` - токен вашего бота
   - `DATABASE_PATH` - `pershcoin.db` (Railway создаст автоматически)

4. **Настройте команду запуска:**
   - Command: `python main.py`
   - Railway автоматически определит Python

5. **Получите URL:**
   - Railway даст вам URL вида: `https://your-app.railway.app`
   - Используйте его как API_BASE

### Плюсы:
- ✅ Очень просто настроить
- ✅ Автоматический деплой из GitHub
- ✅ HTTPS из коробки
- ✅ Бесплатный тариф щедрый

---

## 🌐 Вариант 2: Render.com

**Бесплатно:** Бесплатный тариф с ограничениями

### Шаги:

1. **Зарегистрируйтесь на [Render.com](https://render.com)**
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - New → Web Service
   - Подключите ваш GitHub репозиторий

3. **Настройки:**
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python main.py`
   - Environment: `Python 3`

4. **Добавьте переменные окружения:**
   - `BOT_TOKEN`
   - `DATABASE_PATH`

5. **Получите URL:**
   - Render даст URL: `https://your-app.onrender.com`
   - Используйте его как API_BASE

### Плюсы:
- ✅ Бесплатный тариф
- ✅ Автоматический деплой
- ⚠️ Может "засыпать" после неактивности (просыпается при запросе)

---

## 🚁 Вариант 3: Fly.io

**Бесплатно:** Бесплатный тариф с ограничениями

### Шаги:

1. **Установите Fly CLI:**
   ```bash
   # Windows (PowerShell)
   iwr https://fly.io/install.ps1 -useb | iex
   ```

2. **Зарегистрируйтесь:**
   ```bash
   fly auth signup
   ```

3. **Создайте файл `fly.toml`:**
   ```toml
   app = "pershcoin-api"
   primary_region = "iad"

   [build]

   [http_service]
     internal_port = 8080
     force_https = true
     auto_stop_machines = true
     auto_start_machines = true
     min_machines_running = 0

   [[services]]
     protocol = "tcp"
     internal_port = 8080
   ```

4. **Деплой:**
   ```bash
   fly launch
   fly secrets set BOT_TOKEN=your_token
   ```

5. **Получите URL:**
   - `https://pershcoin-api.fly.dev`

---

## 📝 Вариант 4: Replit

**Бесплатно:** Бесплатный тариф

### Шаги:

1. **Создайте аккаунт на [Replit.com](https://replit.com)**
2. **Создайте новый Repl:**
   - Import from GitHub
   - Выберите ваш репозиторий

3. **Настройте Secrets:**
   - Secrets → Add secret
   - `BOT_TOKEN`

4. **Создайте файл `.replit`:**
   ```
   run = "python main.py"
   ```

5. **Запустите:**
   - Replit даст URL: `https://your-app.repl.co`

---

## ⚙️ После настройки API сервера:

1. **Обновите `webapp/script.js`:**
   ```javascript
   API_BASE = 'https://your-app.railway.app'; // или ваш URL
   ```

2. **Загрузите обновленный `script.js` на GitHub Pages**

3. **Готово!** Ваше приложение будет работать с реальной базой данных

---

## 🎯 Рекомендация:

**Используйте Railway.app** - самый простой и надежный вариант для начала.

После настройки API сервера, обновите URL в `script.js` и загрузите на GitHub Pages.
