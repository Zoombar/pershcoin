# Pershcoin - Telegram Tap-to-Earn Game

Telegram web-app игра в стиле Notcoin, где пользователи зарабатывают монеты тапами.

## Функции

- 🪙 Тап для заработка монет (1 монета за тап)
- 💰 Реферальная система (500 монет новичку, 1000 рефереру, 15% с тапов)
- 📊 Лидерборд (топ игроков по монетам и тапам)
- 📱 Современный web-app интерфейс
- ⚡ Rate limiting (защита от спама)

## Установка

### 1. Клонирование и установка зависимостей

```bash
# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

Заполните `.env`:
- `BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather)
  - Откройте [@BotFather](https://t.me/BotFather) в Telegram
  - Выполните `/newbot` и следуйте инструкциям
  - Скопируйте токен в `.env`
- `DATABASE_PATH` - путь к файлу БД (по умолчанию `pershcoin.db`)
- `WEBAPP_URL` - URL вашего web-app (см. раздел "Развертывание Web-App")
- `TAP_RATE_LIMIT` - лимит тапов в секунду (по умолчанию 10)

### 3. Запуск бота

```bash
python main.py
```

Бот запустится и будет слушать команды. HTTP API сервер запустится на `localhost:8080`.

## Развертывание Web-App

### Вариант 1: GitHub Pages (рекомендуется для начала)

1. Создайте репозиторий на GitHub
2. Загрузите файлы из папки `webapp/` в корень репозитория
3. В настройках репозитория включите GitHub Pages:
   - Settings → Pages → Source: `main` branch
4. Получите URL вида: `https://username.github.io/repository-name/`
5. Обновите `WEBAPP_URL` в `.env`
6. В `webapp/script.js` обновите `API_BASE` на URL вашего сервера с API

### Вариант 2: Свой сервер

1. Загрузите файлы из `webapp/` на ваш веб-сервер
2. Обновите `API_BASE` в `webapp/script.js` на URL вашего API сервера
3. Убедитесь, что ваш API сервер доступен извне

### Настройка Web-App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Выполните `/mybots` → выберите вашего бота
3. Выберите "Bot Settings" → "Menu Button"
4. Выберите "Configure menu button"
5. Введите название кнопки (например, "Играть")
6. Введите URL вашего web-app (например, `https://username.github.io/pershcoin/`)

## Структура проекта

```
pershcoin/
├── main.py                 # Точка входа, запуск бота и HTTP API
├── bot/                    # Логика бота
│   ├── config.py           # Конфигурация
│   ├── keyboards.py        # Клавиатуры
│   └── handlers/          # Обработчики
│       ├── start.py        # Команда /start
│       ├── commands.py     # Команды бота
│       └── webapp.py       # HTTP API для web-app
├── database/               # Работа с БД
│   ├── models.py          # Модели SQLAlchemy
│   ├── database.py        # Подключение к БД
│   └── crud.py            # CRUD операции
├── webapp/                 # Frontend
│   ├── index.html         # Главная страница
│   ├── style.css          # Стили
│   └── script.js          # Логика и API запросы
├── requirements.txt        # Зависимости Python
├── .env.example           # Пример переменных окружения
└── README.md              # Документация
```

## API Endpoints

HTTP API сервер запускается на `localhost:8080` (или на вашем сервере):

- `GET /api/user?initData=...` - получить данные пользователя
- `POST /api/tap` - обработать тап (body: `{"initData": "..."}`)
- `GET /api/leaderboard?initData=...` - получить лидерборд
- `GET /api/referrals?initData=...` - получить список рефералов

## Команды бота

- `/start` - начать игру или обновить данные
- `/balance` - показать баланс и статистику
- `/referral` - показать реферальную информацию
- `/leaderboard` - показать лидерборд
- `/help` - справка по командам

## Механика игры

### Тап
- Каждый тап = +1 монета
- Rate limiting: максимум 10 тапов в секунду
- Анимация и вибрация при тапе

### Реферальная система
- При регистрации по реферальной ссылке:
  - Новый пользователь получает **500 монет**
  - Реферер получает **1000 монет**
- Реферер получает **15%** от каждого тапа реферала (округляется вниз)

### Лидерборд
- Топ-10 игроков по монетам
- Топ-10 игроков по тапам
- Обновление в реальном времени

## Разработка

### Локальная разработка

1. Запустите бота локально: `python main.py`
2. Для тестирования web-app локально:
   - Используйте ngrok или аналогичный сервис для туннелирования
   - Или используйте Telegram Desktop с локальным сервером

### Продакшен

1. Разверните бота на сервере (VPS, Heroku, и т.д.)
2. Настройте веб-сервер (nginx) для проксирования запросов к API
3. Загрузите web-app на статический хостинг
4. Обновите URL в конфигурации

## Лицензия

MIT
